╔══════════════════════════════════════════════════════════════════════════════╗
║                         AI SERVICE API DOCUMENTATION                         ║
║                                  VERSION 4.8.0                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

BASE URL: http://localhost:8000
SWAGGER UI: http://localhost:8000/docs
REDOC: http://localhost:8000/redoc

════════════════════════════════════════════════════════════════════════════════
XÁC THỰC (AUTHENTICATION)
════════════════════════════════════════════════════════════════════════════════

Hệ thống bảo vệ API bằng JWT Token và phân quyền vai trò (RBAC). 
Việc xác thực được thực hiện thông qua gRPC call tới NestJS Backend.

ROLE LEVELS:
  - 🔒 admin: Toàn quyền truy cập, quản lý Metadata và Tệp tin.
  - 🔒 lecture: CHỈ xem được các tệp có gắn nhãn `lecture`. (Không thấy tệp Student hay Nội bộ).
  - 🔒 student: CHỈ xem được các tệp có gắn nhãn `student`. (Không thấy tệp Lecture hay Nội bộ).

HEADER BẮT BUỘC:
  Authorization: Bearer <jwt_token>

LỖI THƯỜNG GẶP:
  - 401 Unauthorized: Thiếu token hoặc token hết hạn.
  - 403 Forbidden: Token hợp lệ nhưng Role không có quyền thực thi.

════════════════════════════════════════════════════════════════════════════════
MỤC LỤC
════════════════════════════════════════════════════════════════════════════════
 1. Health & Status            (2 endpoints)
 2. Email Classification       (2 endpoints)
 3. Metadata Type Management   (9 endpoints) [🔒 admin / 🔒 auth]
 4. File Management APIs       (8 endpoints) [🔒 admin / 🔒 auth]
 5. Chat & RAG Tooling         (3 endpoints) [🔒 auth]
 6. Debug & Preview APIs       (2 endpoints) [🔒 admin]
 7. Real-time Notifications    (1 websocket) [🔒 auth]
 8. Appendix: Error Codes & Formatting

TỔNG: 27 ENDPOINTS

════════════════════════════════════════════════════════════════════════════════
                            1. HEALTH & STATUS
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /health                                                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Kiểm tra tình trạng sức khỏe của service và các kết nối phụ thuộc.    │
│                                                                              │
│ REQUEST: GET /health                                                         │
│                                                                              │
│ RESPONSE 200 OK:                                                             │
│   {                                                                          │
│     "status": "healthy",                                                     │
│     "service": "AI Service",                                                 │
│     "version": "4.8.0",                                                      │
│     "geminiApiConnected": true,                                              │
│     "mongodbConnected": true,                                                │
│     "qdrantConnected": true,                                                 │
│     "redisConnected": true                                                   │
│   }                                                                          │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Thử gọi lệnh ping tới MongoDB.                                          │
│   2. Thử khởi tạo Gemini client để kiểm tra API Key.                         │
│   3. Trả về trạng thái "healthy" nếu tất cả kết nối OK.                      │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /                                                                        │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Root endpoint xác nhận service đang chạy.                            │
│ REQUEST: GET /                                                               │
│ RESPONSE 200 OK: { "message": "AI Service is running" }                      │
│ LUỒNG XỬ LÝ: Trả về thông báo tĩnh xác nhận web service đang lắng nghe.      │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        2. EMAIL CLASSIFICATION
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/email/process                                                      │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Phân loại và trích xuất thông tin từ tiêu đề và nội dung email.       │
│                                                                              │
│ REQUEST EXAMPLE:                                                             │
│   {                                                                          │
│     "title": "Hỏi về quy trình tốt nghiệp",                                  │
│     "content": "Chào thầy cô, khóa K62 khi nào làm đồ án?"                   │
│   }                                                                          │
│                                                                              │
│ RESPONSE 200 OK:                                                             │
│   {                                                                          │
│     "label": "inquiry",                                                      │
│     "inquiry": { "answer": "...", "sources": [...] }                         │
│   }                                                                          │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Controller nhận title/content -> Gọi EmailWorkflowOrchestrator.         │
│   2. AI phân loại email thành 1 trong 4 nhãn chuẩn.                          │
│   3. Nếu nhãn là 'Inquiry', thực hiện RAG Search trên kho tài liệu.          │
│   4. Gemini soạn thảo bản thảo phản hồi (Reply Draft) dựa trên context tìm được.│
│   5. Trả về nhãn và dữ liệu bổ sung tương ứng với loại workflow.             │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/email/test/ingested                                                │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Debug xử lý RabbitMQ payload trực tiếp qua HTTP.                      │
│                                                                              │
│ REQUEST: { "pattern": "ingested", "data": { "subject": "...", "content": "..." }}│
│                                                                              │
│ RESPONSE 200 OK: { "success": true, "data": { ... } }                        │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Giả lập việc nhận một message từ queue 'email_ingest_queue'.             │
│   2. Giải nén payload 'data' và truyền vào Orchestrator xử lý như email thực. │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        3. METADATA TYPE MANAGEMENT
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/metadata               [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Tạo một loại nhãn metadata mới để phân loại tài liệu.                 │
│                                                                              │
│ REQUEST EXAMPLE:                                                             │
│   {                                                                          │
│     "key": "department",                                                     │
│     "displayName": "Khoa đào tạo",                                           │
│     "allowedValues": [                                                       │
│       { "value": "cntt", "displayName": "CNTT", "color": "#3498DB" }         │
│     ]                                                                        │
│   }                                                                          │
│                                                                              │
│ RESPONSE 201: { "metadataId": "...", "key": "department", ... }              │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. MetadataService chuẩn hóa key về snake_case.                            │
│   2. Kiểm tra key đã tồn tại trong DB chưa.                                  │
│   3. Lưu định nghĩa vào MongoDB collection `metadata_types`.                 │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/metadata                [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Liệt kê danh sách tất cả các loại nhãn metadata có trong hệ thống.    │
│                                                                              │
│ QUERY PARAMETERS:                                                            │
│   - isActive: bool (optional) - Lọc nhãn đang hoạt động.                     │
│   - keywords: string (optional) - Tìm kiếm tên nhãn.                         │
│                                                                              │
│ RESPONSE 200 OK: { "metadataTypes": [...], "total": 5 }                      │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Controller gọi Service lấy tất cả Metadata Types.                       │
│   2. [MASKING]: Duyệt từng loại nhãn, lọc bỏ các `allowedValues` có          │
│      `visibleRoles` không chứa role của User hiện tại.                       │
│   3. [SECURITY]: Nếu User không phải Admin, ép buộc `isActive=true`.         │
│   4. Trả về danh sách đã được lọc bảo mật.                                   │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/metadata/exists         [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Kiểm tra nhanh xem một Metadata Key đã tồn tại chưa.                  │
│ QUERY PARAMETERS: key=string (Bắt buộc)                                      │
│ RESPONSE 200: { "exists": true }                                             │
│ LUỒNG XỬ LÝ: Truy vấn MongoDB tìm chính xác key đã normalize.                │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/metadata/{key}          [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Lấy thông tin chi tiết một nhãn metadata theo Key.                    │
│ RESPONSE 200 OK: { "key": "...", "allowedValues": [...], ... }               │
│ LUỒNG XỬ LÝ: Áp dụng logic lọc bảo mật (Masking) tương tự như API List.      │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ PATCH /api/metadata/{key}        [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Cập nhật tên hiển thị, mô tả hoặc trạng thái của nhãn.               │
│ REQUEST: { "displayName": "Tên mới", "isActive": false }                      │
│ RESPONSE 200 OK: MetadataType sau khi cập nhật.                              │
│ LUỒNG XỬ LÝ: Tìm nhãn theo key -> Cập nhật các trường cho phép -> Lưu DB.    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/metadata/{key}/values  [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Thêm một giá trị hợp lệ mới vào danh sách `allowedValues` của nhãn.   │
│ REQUEST: { "value": "ctsv", "displayName": "Công tác SV", "color": "#000" }  │
│ RESPONSE 200 OK: MetadataType với mảng values mới.                           │
│ LUỒNG XỬ LÝ: Kiểm tra key -> Kiểm tra value trùng chưa -> Push vào array.    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ PATCH /api/metadata/{key}/values/{value} [🔒 ADMIN]                          │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Sửa thông tin hiển thị hoặc phân quyền của một giá trị nhãn.          │
│ REQUEST: { "visibleRoles": ["lecture"] }                                     │
│ RESPONSE 200 OK: MetadataType đã cập nhật.                                   │
│ LUỒNG XỬ LÝ: Dùng array filters của MongoDB để update phần tử trong mảng.    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ DELETE /api/metadata/{key}/values/{value} [🔒 ADMIN]                         │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Xóa một giá trị khỏi nhãn metadata.                                   │
│ RESPONSE 200 OK: MetadataType sau khi xóa.                                   │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Kiểm tra `totalFiles` của giá trị này trong DB.                         │
│   2. Nếu > 0: Reject (409 Conflict) vì đang có tệp tin sử dụng nhãn này.      │
│   3. Nếu = 0: Xóa phần tử khỏi mảng `allowedValues` của document.            │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ DELETE /api/metadata/{key}       [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Xoá hoàn toàn một định nghĩa nhãn metadata.                           │
│ RESPONSE 204: No Content.                                                    │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Kiểm tra nhãn có phải nhãn hệ thống (isSystem=true) không -> Chặn xóa.   │
│   2. Kiểm tra tổng tệp (totalFiles) của nhãn -> Nếu > 0 reject xóa.          │
│   3. Thực hiện Hard Delete record trong MongoDB.                             │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        4. FILE MANAGEMENT
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/files                  [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Upload tệp tin gốc và kích hoạt quy trình bóc tách RAG tự động.       │
│                                                                              │
│ REQUEST (Multipart):                                                         │
│   - file: <binary>                                                           │
│   - displayName: "Văn bản A"                                                 │
│   - customMetadata: JSON string (vd: '{"access_scope":["student"], "academic_year":["2025-2026"]}') │
│                                                                              │
│ RESPONSE 201: { "fileId": "67...", "status": "uploading", ... }              │
│   - Bao gồm: tableOfContents (list phẳng), summary (tóm tắt văn bản).        │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. FileService.upload_file_quick: Lưu tạm -> Upload R2 -> Tạo Mongo record.│
│   2. Trả về kết quả ngay lập tức kèm trạng thái "uploading".                 │
│   3. Kích hoạt BackgroundTask chạy process_file_background.                  │
│   4. Background: LlamaParse bóc tách -> PageIndex sinh Tree -> Qdrant Embed. │
│   5. Hoàn tất: Sử dụng `find_one_and_update` để chuyển status sang "ready"    │
│      nguyên tử. Chỉ khi transition thành công mới tăng bộ đếm Metadata.      │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/files                   [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Liệt kê tệp tin đính kèm bộ lọc (Dành cho Student/Lecture).           │
│                                                                              │
│ QUERY PARAMETERS:                                                            │
│   - fileStatus: string (ready|processing|failed)                             │
│   - metadataFilter: JSON string (VD: {"cohort":["k63"]})                     │
│   - keywords: string (Tìm theo tên tệp)                                      │
│   - page/limit: Phân trang (mặc định 1/50)                                   │
│                                                                              │
│ RESPONSE 200 OK: { "files": [...], "total": 120 }                            │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Phân tích JWT Rule -> Nếu không phải Admin: ép thêm filter accessScope. │
│   2. Chú ý: Tệp không có nhãn accessScope sẽ bị ẩn đối với Student/Lecture.  │
│   3. FilterBuilder xây dựng MongoDB query từ metadataFilter.                 │
│   4. Truy vấn MongoDB trả về danh sách tệp Metadata Masked.                  │
└──────────────────────────────────────────────────────────────────────────────┘



┌──────────────────────────────────────────────────────────────────────────────┐
│ PATCH /api/files/{file_id}       [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Cập nhật thông tin tệp tin (Tên hiển thị, Metadata).                  │
│                                                                              │
│ REQUEST EXAMPLE:                                                             │
│   {                                                                          │
│     "displayName": "Tên mới.pdf",                                            │
│     "customMetadata": {                                                      │
│       "academicYear": ["2025-2026"],                                         │
│       "accessScope": ["lecture"]                                             │
│     }                                                                        │
│   }                                                                          │
│                                                                              │
│ RESPONSE: FileDetailResponse                                                 │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. MongoDB: Cập nhật thông tin record file.                                │
│   2. Qdrant Sync: Nếu có cập nhật `customMetadata`, hệ thống tự động đồng bộ │
│      lại payload của tất cả các vector points thuộc file này trên Qdrant.    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/files/batch            [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Upload đồng thời nhiều tệp tin.                                       │
│ REQUEST: - files: [binary], - displayNames: JSON, - metadataList: JSON       │
│ RESPONSE: { "total": 5, "successful": 4, "failed": 1, "results": [...] }     │
│ LUỒNG XỬ LÝ: Chạy vòng lặp gọi hàm upload đơn lẻ cho từng thành phần.        │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/toc-tree/{fileId}       [🔒 AUTH]                                   │
│ ├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Lấy cấu trúc mục lục (TOC Tree) chi tiết theo PageIndex Schema.        │
│ RESPONSE: { "docName": "...", "structure": [...], "markdownStoragePath": "..." } │
│ LUỒNG XỬ LÝ: Truy vấn collection `file_toc_trees` theo fileId.                │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/files/{fileId}          [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Lấy thông tin chi tiết một tệp tin.                                   │
│ RESPONSE: FileDetailResponse kèm Download URLs, tableOfContents và summary.  │
│ LUỒNG XỬ LÝ: Fetch Mongo -> Generate link tải R2 (Public hoặc Presigned).    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ GET /api/files/{fileId}/download [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Tải xuống trực tiếp tệp tin binary từ kho lưu trữ R2.                 │
│ RESPONSE: Binary file stream.                                                │
│ LUỒNG XỬ LÝ: FileService tải bytes từ R2 và trả về qua StreamingResponse.    │
└──────────────────────────────────────────────────────────────────────────────┘
│ DELETE /api/files/{fileId}       [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Xóa vĩnh viễn tệp tin khỏi mọi kho lưu trữ.                           │
│ RESPONSE 204: No Content.                                                    │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   1. Qdrant: Xóa toàn bộ Vector points mang file_id tương ứng.               │
│   2. R2: Xóa binary file gốc và binary file markdown bóc tách.                │
│   3. Metadata Service: Sử dụng `find_one_and_delete` nguyên tử. Nếu tệp bị xóa│
│      đang ở trạng thái "ready", hệ thống mới thực hiện trừ bộ đếm Metadata.  │
│   4. MongoDB: Xóa bản ghi tệp và bản ghi TOC Tree.                           │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        5. CHAT & RAG TOOLING
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/chat/query             [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Hỏi đáp thông minh đồng bộ trên kho tài liệu bóc tách.                │
│                                                                              │
│ REQUEST BODY:                                                                │
│   {                                                                          │
│     "question": "Quy trình xin thôi học?",                                   │
│     "chatHistory": [],                                                       │
│     "metadataFilter": {"academicYear": ["2025"]}                             │
│   }                                                                          │
│                                                                              │
│ RESPONSE 200 OK:                                                             │
│   {                                                                          │
│     "answer": "Theo quyết định số...",                                       │
│     "sources": [{"citationId": 1, "title": "HD01", "url": "..."}],           │
│     "processingTimeMs": 2500                                                 │
│   }                                                                          │
│                                                                              │
│ LUỒNG XỬ LÝ (WORKFLOW):                                                      │
│   0. RAG Gate (Gatekeeper): Sử dụng AI phân tích ý định. Nếu là câu chào hỏi │
│      hoặc xã giao, trả lời trực tiếp và BYPASS hoàn toàn luồng RAG tốn kém.  │
│   1. RAG Service: Vector search lấy Top-30 chunks (Coarse Search).           │
│   2. PageIndex Semantic Reranking: Tính toán DocScore từng File dựa trên     │
│      mẫu 30 chunks này để chọn ra Top-5 Files tốt nhất (Fine Selection).     │
│   3. Build Context: Kết hợp descriptions của Files tìm được và lịch sử chat. │
│   4. Agentic Loop: LLM quyết định dùng Tools (PageIndex) để đọc sâu tài liệu.│
│   5. Finalize: Giải đáp câu hỏi và tổng hợp nguồn trích dẫn.                 │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/chat/stream            [🔒 AUTH]                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Hỏi đáp RAG thời gian thực qua Web Stream (SSE).                      │
│                                                                              │
│ RESPONSE (Event Stream):                                                     │
│   - Thought: data: {"type": "thought", "content": "thinking...", "done": false}  │
│   - Text:    data: {"type": "text", "content": "Hello", "done": false}           │
│   - Call:    data: {"type": "call", "name": "get_doc", "args": {...}, "done": false} │
│   - Final:   data: {"done": true, "sources": [...], "processingTimeMs": 1234}    │
│                                                                              │
│ LUỒNG XỬ LÝ: Tương tự `/query` nhưng response được chunking ngay lập tức.    │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/chat/retrieve-preview  [🔒 ADMIN]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Xem trước các đoạn văn bản (Chunks) mà RAG tìm được cho câu hỏi.      │
│                                                                              │
│ REQUEST: { "question": "...", "topK": 5, "minScore": 0.3 }                   │
│                                                                              │
│ RESPONSE: { "items": [{"rank": 1, "score": 0.85, "text": "...", ...}] }      │
│                                                                              │
│ LUỒNG XỬ LÝ: Thực thi Vector Search nguyên bản trên Qdrant và trả về metadata.│
└──────────────────────────────────────────────────────────────────────────────┘



════════════════════════════════════════════════════════════════════════════════
                        6. DEBUG & PREVIEW APIs
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/debug/files/parse-preview [🔒 ADMIN]                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Thử nghiệm bóc tách PDF sang Markdown (Không lưu tệp vào hệ thống).   │
│ REQUEST: - file: <binary>                                                    │
│ RESPONSE: { "pages": [{"pageIndex": 0, "markdown": "..."}] }                 │
│ LUỒNG XỬ LÝ: Gửi tệp tới LlamaParse API và trả về kết quả thô ngay lập tức.  │
└──────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────────┐
│ POST /api/debug/files/chunk-preview [🔒 ADMIN]                               │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: Thử nghiệm bóc tách kèm chia mảnh (Markdown + Semantic Chunks).       │
│ RESPONSE: { "chunkCount": 25, "chunks": [...] }                              │
│ LUỒNG XỬ LÝ: Gọi LlamaParse sau đó chạy logic ChunkingService cục bộ.        │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        7. REAL-TIME NOTIFICATIONS
════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────────────────────────────────────────────────────────┐
│ WS /api/files/progress/{clientId} [🔒 AUTH]                                  │
├──────────────────────────────────────────────────────────────────────────────┤
│ MÔ TẢ: WebSocket theo dõi tiến trình upload và xử lý file thời gian thực.    │
│                                                                              │
│ LUỒNG XỬ LÝ:                                                                 │
│   1. Client kết nối với clientId duy nhất (trùng clientId lúc gọi POST file).│
│   2. Hệ thống đẩy JSON status updates theo từng bước:                        │
│      - {"step": "db_creating", "message": "...", "file_id": "..."}           │
│      - {"step": "parsing_markdown", ...}                                     │
│      - {"step": "ready", ...}                                                │
│   3. Socket tự động đóng khi hoàn tất hoặc lỗi.                              │
└──────────────────────────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════
                        7. APPENDIX: ERROR CODES
════════════════════════════════════════════════════════════════════════════════

- 400 Bad Request: Lỗi tham số (ValidationException).
- 401 Unauthorized: JWT token không hợp lệ (gRPC Auth reject).
- 403 Forbidden: Sai Role người dùng (ForbiddenException).
- 404 Not Found: Dữ liệu không tồn tại trong DB.
- 409 Conflict: Dữ liệu đã tồn tại hoặc đang bị ràng buộc (Metadata in use).
- 500 Internal Error: Lỗi hạ tầng (Qdrant, MongoDB, R2, Gemini API).

--------------------------------------------------------------------------------
LƯU Ý VỀ LƯU TRỮ VÀ CACHING (STORAGE & CACHE):
- Tài liệu Markdown (.md) được cache tại local server (`PAGEINDEX_WORKSPACE/artifacts`).
- Thời gian tồn tại (TTL): 60 phút kể từ lần truy cập cuối.
- Background Task: Hệ thống tự động quét và dọn dẹp các tệp quá hạn mỗi 30 phút.
- R2 Fallback: Nếu cache bị dọn dẹp, hệ thống sẽ tự động tải lại từ R2 khi có yêu cầu.
- Agent Control: Số vòng lặp tối đa của RAG Agent được cấu hình qua `AGENT_MAX_TURNS` (mặc định: 7).

Hoàn tất bản Đặc tả API chi tiết v4.8.0 (Full Compliance).
--------------------------------------------------------------------------------
