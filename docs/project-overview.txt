================================================================================
AI SERVICE — PROJECT OVERVIEW (EXTREME DETAIL)
Version 5.5.0 | May 2026
================================================================================

MUC LUC (DETAILED INDEX)
──────────────────────────────────────────────────────────────────────────────
 1.  Tổng quan & Mục đích (Mission Statement)
 2.  Kiến trúc hệ thống Modular RAG (High-level & Component Interaction)
 3.  Cấu trúc thư mục chi tiết (File-by-file Responsibility)
 4.  Lifespan & Startup Logic (Bản đồ khởi động hệ thống)
 5.  Luồng xử lý Email (RabbitMQ Consumer & Ingest Engine)
 6.  LabelClassifierService (AI Triage & Keywords Pre-check)
     - 6.1. Chi tiết Prompt phân loại (Labeling Prompt)
     - 6.2. Thuật toán Normalize Label & Overrides
 7.  LLM Factory (Gemini Integration & Settings)
     - 7.1. Thinking Levels (None, Low, Medium, High)
     - 7.2. Model Configuration (Temperature, Max Tokens, TopP/K)
 8.  Hệ thống Workflows & gRPC Service Integration
     - 8.1. ClassRegistrationService (Extraction & Ticket creation)
     - 8.2. InquiryService (Intent, RAG Reply, Gmail Draft)
     - 8.3. TaskService (User Lookup & Task dispatching)
     - 8.4. OtherService (Log & Sink)
 9.  Forms Management Module (Unified Links & Excel Processing)
 10. Email Draft Service (RAG-based Reply Generation)
 11. Modular RAG Ingestion Pipeline (Phase-by-Phase)
     - 11.1. R2 Storage (Original Document Storage)
     - 11.2. LlamaParse (PDF-to-Markdown High-Quality Conversion)
     - 11.3. PageIndex Indexing (Structural Tree & Description)
     - 11.4. Qdrant Indexer (Semantic Chunking & Qdrant Push)
 12. FAQ Module v2 (Semantic FAQ & Auto-Synthesis)
 13. Hệ thống Metadata Types (Definitions & Rules)
     - 13.1. System Metadata (academicYear, cohort)
     - 13.2. ATOMIC COUNTER LOGIC (IDEMPOTENCY)
 14. Filter Builder Logic (Metadata-to-Vector Query Mapping)
     - 14.1. Metadata Filter Construction Logic
 15. RAG Service (Strategy & Search Flow)
 16. PageIndex Integration — Agentic Tools (get_document, tree, content)
 17. Chat Service & Agentic RAG logic
     - 17.1. SSE (Server-Sent Events) Streaming Protocol details
 18. Auth Service & Security Layer
     - 18.1. gRPC VerifyToken Request & Response payload mapping
 19. Cấu trúc MongoDB Schema (Detailed Field-by-field)
     - 19.1. Collection: files
     - 19.2. Collection: file_toc_trees
     - 19.3. Collection: metadata_types
     - 19.4. Collection: faqs
     - 19.5. Collection: faq_candidates
 20. Repository Layer & BaseRepository CRUD pattern
 21. Storage Management (Cloudflare R2 Service details)
 22. RabbitMQ Ingest Consumer (Consumer Resilience & Retry Logic)
 23. Hệ thống Exception Hierarchy & HTTP Status Mapping
 24. Danh lục REST API Endpoints đầy đủ
 25. Real-time Notifications (WebSocket Progress channel)
 26. Cấu hình hệ thống (Settings Table & .env impacts)
 27. Scripts hỗ trợ bảo trì (Maintenance & Debug Tools)
 28. Phụ lục: Quy chuẩn xử lý Excel & Rich Text (Integration/Excel)


================================================================================
1. TONG QUAN & MUC DICH (MISSION STATEMENT)
================================================================================

AI Service là một FastAPI microservice hiệu năng cao, đóng vai trò là \"Hệ điều hành AI\" trung tâm cho các nghiệp vụ giáo vụ. Dự án được thiết kế để xử lý các tài liệu học vụ phức tạp và tự động hóa quy trình phản hồi email từ sinh viên/giảng viên.

HỆ THỐNG GIẢI QUYẾT 2 BÀI TOÁN LỚN:
1.  EMAIL TRIAGE & AUTOMATION: Tự động phân loại hàng nghìn email đổ về từ NestJS, trích xuất dữ liệu đăng ký môn học hoặc tự động soạn thảo bản thảo phản hồi (Email Draft) dựa trên kho tài liệu thực tế của nhà trường.
2.  MODULAR RAG DOCUMENT BRAIN: Xây dựng một \"bộ não\" kiến thức từ hàng nghìn văn bản quy định (.pdf, .md, .docx). Hệ thống không chỉ tìm kiếm từ khóa mà hiểu được \"Mục lục\" và \"Cấu trúc\" tài liệu để trả lời chính xác từng trang, từng dòng.


================================================================================
2. KIEN TRUC HE THONG MODULAR RAG
================================================================================

Dự án sử dụng kiến trúc RAG Modular (phi tập trung), kiểm soát hoàn toàn dữ liệu với luồng pipeline mới (State-free):

SƠ ĐỒ LUỒNG PHỐI HỢP COMPONENT (Upload -> R2 -> LlamaParse -> PageIndex (TOC) -> Qdrant):
1.  UPLOAD LAYER: Nhận file -> Validate Metadata (Required fields) -> R2 (Lưu file gốc) -> MongoDB (Status: uploading).
    - [NEW]: Hỗ trợ clientId để đẩy thông báo tiến trình real-time qua WebSocket.
    - [NEW]: Mọi API Multipart/Form-data đều hỗ trợ song song `snake_case` và `camelCase` cho các trường dữ liệu đầu vào.
2.  INDEXING LAYER (ASYNC):
    - LlamaParse: Trình phân tích PDF tốt nhất hiện nay, xử lý được bảng biểu (tables) trong quy chế.
    - PageIndex: Đóng vai trò kiến trúc sư mục lục. Nó không lấy text thô mà xây dựng một cây Phân cấp (Building an Heirarchical Tree).
    - Qdrant Indexer (app/pipelines/ingestion/chunking.py): Chịu trách nhiệm chia mảnh (Chunking) và đẩy Vector lên Qdrant kèm Metadata gắn sẵn.
3.  RAG LAYER:
    - Khi có Query: Hệ thống thực hiện Vector search dựa trên query ngữ nghĩa.
    - Qdrant: Trả về Top-K Chunks liên quan nhất.
    - MongoDB: Trả về "doc_description" (1 câu tóm tắt file) để AI định hướng context.
4.  AGENTIC CHAT LAYER:
    - [NEW] RAG Gate: Hệ thống sử dụng AI để đánh giá câu hỏi. Nếu là câu hỏi xã giao (Small Talk), AI sẽ trả lời trực tiếp mà không cần gọi RAG, giúp tiết kiệm tài nguyên.
    - AI Agent (Gemini) không nhận hàng tấn text. Nó nhận danh sách File IDs.
    - Agent sử dụng bộ công cụ (Tools) để gọi lại PageIndex: \"Hãy cho tôi xem mục lục File ID 1\" -> \"Tôi muốn đọc trang 5 của File ID 1\".


================================================================================
3. CAU TRUC THU MUC CHI TIET (FILE-BY-FILE RESPONSIBILITY)
================================================================================

Dự án được tổ chức theo module chức năng (Domain-Driven style):

app/
├── core/
│   ├── config.py             : Quản lý biến môi trường (.env) qua Pydantic Settings.
│   ├── database.py           : Cấu hình Motor (Async MongoDB), khởi tạo indexes.
│   ├── exceptions.py         : Hệ thống Exception tập trung.
│   └── converters.py         : Logic chuyển đổi kiểu dữ liệu (vd: camelCase converters cho API).
├── integrations/ (External Gateways - Pure Clients)
│   ├── llamaparse/           : Client gọi Cloud LlamaParse API.
│   ├── pageindex/            : Kết nối PageIndex engine (TOC Tree, Agent Tools).
│   ├── qdrant/               : Client cấp thấp cho Qdrant Vector DB.
│   ├── storage/              : Client Cloudflare R2 (aioboto3).
│   ├── grpc/                 : Client gRPC giao tiếp Nest-api.
│   ├── rabbitmq/             : Logic kết nối và consume RabbitMQ.
│   ├── llm/                  : Factory khởi tạo Gemini/GenAI SDK.
│   └── excel/                : [NEW] Tiện ích xử lý Excel (Rich Text, Numeric Formatting).
├── modules/ (Business Domain Logic)
│   ├── files/                : Quản lý tệp tin, upload, delete, metadata sync.
│   │   └── toc_tree/         : [Moved] Repo & Model chuyên biệt cho cấu trúc tài liệu.
│   ├── chat/                 : Controller của Agentic Chat & SSE Streaming.
│   ├── rag/                  : RAG Service & Tool-use Agent logic.
│   ├── email/                : Hệ thống phân loại & Workflows xử lý (Inquiry, etc.).
│   ├── metadata/             : Quản lý Metadata Types & Validation.
│   ├── faq/                  : Quản lý FAQ, Synthesis (Interaction Logs Clustering).
│   └── forms/                : [NEW] Quản lý biểu mẫu học thuật & Links.
├── pipelines/ (Multi-step Async Processes)
│   └── ingestion/            : [NEW] Toàn bộ quy trình nạp liệu (LlamaParse -> Chunking -> Vector Push).
├── utils/                    : Các tiện ích dùng chung (JSON, Text, Format utils).
├── repositories/             : Implement lớp truy cập dữ liệu (BaseRepository pattern).
├── proto/                    : Protobuf source và generated code.

scripts/
├── init_db.py                : Script khởi tạo hệ thống (Xóa DB cũ, Index mới, Clear Qdrant/R2).
├── seed_metadata.py          : Nạp dữ liệu cấu hình ban đầu (Academic Year, Cohort).
├── run_all.sh                : Suite kiểm thử toàn diện integration tests.


================================================================================
4. LIFESPAN & STARTUP LOGIC
================================================================================

Hệ thống sử dụng FastAPI Lifespan (app/main.py) để quản lý khởi động an toàn:

BẢN ĐỒ KHỞI ĐỘNG (BƯỚC-BẰNG-BƯỚC):
1.  DATABASE CONNECT: Khởi tạo pool kết nối MongoDB (Motor). Test kết nối bằng lệnh 'ping'.
2.  ENSURE INDEXES: 
    - FILES collection: `idx_file_display_name_regex`.
    - FILE_TOC_TREES collection: `idx_file_toc_trees_fileId` (Unique=True).
    - METADATA_TYPES collection: `idx_metadata_types_key` (Unique=True).
3.  STORAGE WARMUP: Khởi tạo R2 client. Nếu lỗi chỉ log warning (Safe bypass) để cho phép app khởi động ngay cả khi storage tạm lỗi.
4.  RABBITMQ BINDING: Declare queue `email_ingest_queue` và bind exchange nếu cần.
5.  GRPC CLIENT: Khởi tạo persistent channel tới Nest-api.
6.  START BACKGROUND CONSUMER: Chạy `EmailIngestConsumer` trên daemon thread. Thử lại tối đa 10 lần nếu rabbitmq connection bị die lúc startup.


================================================================================
5. LUONG XU LY EMAIL (RABBITMQ INGEST ENGINE)
================================================================================

Đầu vào từ RabbitMQ (Payload IngestMessage):
  - field: data.messageId (int)
  - field: data.subject (str)
  - field: data.content (str)
  - field: data.senderEmail/senderName (str)

LUỒNG XỬ LÝ NỘI BỘ (EmailIngestConsumer.py):
1.  Nhận message từ queue -> Ack ngay lập tức nếu JSON lỗi (Anti-Poison).
2.  Validate mảng `data` qua Pydantic.
3.  Dispatcher: Gọi `EmailWorkflowOrchestrator.process_request`.
4.  Async Execution: Sử dụng `asyncio.run_coroutine_threadsafe` gửi coroutine vào loop chính của FastAPI.
5.  Timeout Control: Mỗi email có tối đa 120 giây xử lý trước khi bị ngắt.
6.  Final ACK/NACK:
    - OK -> Xóa khỏi queue.
    - Error (Database/LLM sập) -> Requeue=True để chờ hệ thống hồi phục.


================================================================================
6. LABELCLASSIFIERSERVICE (AI TRIAGE)
================================================================================

6.1. CHI TIẾT PROMPT PHÂN LOẠI (SYSTEM PROMPT):
AI nhận vai trò \"Senior Triage Assistant\" với các chỉ dẫn nghiêm ngặt:
- classRegistration: Yêu cầu hành động về học phần (đăng ký, hủy, xin mở lớp). Keyword: \"mã học phần\", \"học phần\", \"mở lớp\".
- task: Yêu cầu từ đơn vị nội bộ (hành chính, khoa) giao việc cho phòng giáo vụ.
- inquiry: SV hỏi đáp về quy định (QA). Không yêu cầu hành động đăng ký trực tiếp.
- other: Spam, lời chào, nội dung không liên quan.

RÀNG BUỘC CỦA PROMPT:
- Trả về DUY NHẤT một token nhãn chuẩn.
- Không giải thích, không markdown.
- Ưu tiên: Registration > Task > Inquiry > Other.

6.2. THUẬT TOÁN NORMALIZE & OVERRIDES:
- AI thường trả về \"classRegistration.\" (có chấm) hoặc \"Label: task\".
- Logic Normalize (Python regex) sẽ bóc tách chỉ giữ lại Alphabet, chuyển về lowercase và map vào bộ alias chuẩn.
- Override Heuristic: Nếu AI trả về 'other' nhưng trong email chứa từ khóa \"đăng ký học phần\", hệ thống sẽ ép kiểu về `classRegistration` để đảm bảo không bỏ sót yêu cầu của SV.


================================================================================
7. LLM FACTORY (GEMINI INTEGRATION)
================================================================================

Dự án sử dụng Gemini (Google GenAI Python SDK v1.x) làm engine AI duy nhất.

7.1. THINKING LEVELS (QUẢN LÝ SUY LUẬN):
- none : Không dùng Thinking, rẻ và nhanh nhất. Thích hợp phân loại email ngắn.
- low/medium: Bật `ThinkingConfig` với tokens giới hạn (~1024-4096). Tăng độ chính xác khi trích xuất Ticket đăng ký học phần từ email dài.
- high: Dùng cho các tác vụ phân tích tài liệu (PageIndex description generation).

7.2. MODEL SETTINGS (CẤU HÌNH API):
- Temperature 0.1: Dùng cho triết xuất (Extraction) để tránh AI \"sáng tạo\" thêm thông tin SV.
- Temperature 0.7: Dùng cho Chat/Email Draft để câu trả lời tự nhiên.
- Timeout 60-90s: Đảm bảo app không bị treo nếu Gemini phản hồi chậm.
- Model Pass-through: Hỗ trợ tự động thêm tiền tố \"gemini/\" cho model nếu dùng qua LiteLLM gateway.


================================================================================
8. HE THONG WORKFLOWS & GRPC INTEGRATION
================================================================================

Dự án giao tiếp với Nest-api thông qua một `GrpcClient` singleton (app/integrations/grpc/client.py).

8.1. CLASSREGISTRATION SERVICE:
- Logic: AI nhận email -> Trích xuất mảng JSON items (action, subjectCode, className...).
- Mapping: Tự động chuẩn hóa `messageId` từ email ban đầu.
- gRPC Call: Gọi `rpc Create` của `ClassRegistrationServiceStub`.
- Payload: Chứa đầy đủ các trường Pydantic của NestJS để tạo Entry hỗ trợ SV.

8.2. INQUIRY SERVICE:
- B1: Gọi Gemini trích xuất `intent` (vấn đề thực sự) và các bộ lọc Metadata (Khóa, Năm học).
- B2: [NEW] FAQ Pre-check: Tìm kiếm ngữ nghĩa trong FAQ Module v2. Hệ thống giờ đây lấy context của 5 FAQ gần nhất để đảm bảo tính nhất quán về văn phong và nội dung. Nếu có câu trả lời sẵn -> Trả về ngay lập tức để tiết kiệm tài nguyên.
- B3: Nếu FAQ không khớp, hệ thống gọi RAG service lấy context từ tài liệu trường dựa trên Vector Search.
- B4: Gemini soạn câu trả lời (Email Draft Body). Đặc biệt: Các trích dẫn `(^...)` được tự động chuyển thành Link Markdown dẫn đến file gốc (original_url) của tài liệu. Ngoài ra, hỗ trợ tham số `to_rich_text` để tự động convert sang HTML cho Client.
- gRPC Call: Gọi `rpc Create` của `InquiryServiceStub` để ghi nhận thắc mắc và lưu bản thảo email.

8.3. TASK SERVICE:
- Logic: AI trích xuất `name`, `description`, `due` (deadline) và `assignee_keywords` (ví dụ: "cô Lan").
- Resolver: Chạy song song (`asyncio.gather`) các cuộc gọi gRPC `FindOneByKeyword` tới `AuthServiceStub` để lấy User ID thực tế.
- gRPC Call: Gọi `rpc Create` của `TaskServiceStub` để tạo task và gán người thực hiện.


================================================================================
9. EMAIL DRAFT SERVICE (RAG-BASED REPLY)
================================================================================

Dịch vụ này không phải là API public, nó là trái tim của Inquiry Workflow.

CẤU TRÚC PROMPT SOẠN EMAIL (EMAIL_DRAFT_REPLY_PROMPT):
- Vai trò: Đại diện phòng giáo vụ.
- Yêu cầu: Không dùng lời chào/lời kết (vì NestJS sẽ tự thêm Template).
- Ràng buộc: Câu đầu tiên phải đi thẳng vào nội dung trả lời dựa trên tài liệu.
- Định dạng: Markdown (Header, Bold) để trình bày đẹp mắt.
- Nội dung: Tự động tóm tắt các `Sources` (Nguồn trích dẫn) vào cuối nội dung để SV kiểm chứng.


================================================================================
11. MODULAR RAG INGESTION PIPELINE (PIHASE-BY-PHASE)
================================================================================

[Chi tiết RAG ingestion không đổi, đã tóm tắt phía trên]


================================================================================
12. FAQ MODULE V2 (SEMANTIC FAQ & AUTO-SYNTHESIS)
================================================================================

Module FAQ cung cấp một lớp "Pre-check" và bộ nhớ tập trung cho các câu hỏi thường gặp, giúp giảm tải cho LLM và tăng tốc độ phản hồi.

11.1. MANUAL FAQ MANAGEMENT (Quản lý thủ công):
- Hỗ trợ thao tác CRUD (Tạo, Sửa, Xóa) cho FAQ.
- Semantic Search: Sử dụng Gemini Embeddings để match câu hỏi người dùng với FAQ có sẵn thông qua Qdrant (cosine similarity).
- Danh sách FAQ giờ đây được sắp xếp mặc định theo `createdAt` (giảm dần).
- Metadata Filter: Hỗ trợ lọc theo `academicYear` và `cohort`. Nếu mảng rỗng (empty array), FAQ áp dụng cho toàn bộ sinh viên.
- [NEW] Bulk Management: 
  - Hỗ trợ Import hàng loạt từ file Excel (.xlsx) với cơ chế map cột linh hoạt (theo tên hoặc ký tự).
  - [FORMATTING]: Hệ thống tự động bảo toàn định dạng Rich Text (In đậm, In nghiêng, Gạch chân) và các Hyperlink từ Excel, chuyển đổi chúng sang định dạng Markdown chuẩn.
  - Kỹ thuật: Sử dụng `openpyxl` với tham số `rich_text=True` để truy cập các fragments văn bản và thuộc tính font, kết hợp xử lý `cell.hyperlink`.
  - Tự động bóc tách Metadata từ các cột Excel chỉ định.
  - Tối ưu hiệu suất: Sử dụng `asyncio.gather` để sinh Embedding song song cho toàn bộ danh sách câu hỏi.
  - Cơ chế Skip & Continue: Tự động bỏ qua câu hỏi trùng lặp và ghi nhận chi tiết các dòng lỗi mà không làm gián đoạn tiến trình import chung.
  - Hỗ trợ API Bulk Create qua JSON để tích hợp với các hệ thống bên ngoài.

11.2. INTERACTION LOGGING (Theo dõi tương tác):
- Hệ thống tự động ghi lại lịch sử Chat và Email Inquiry thành `InteractionLogs`.
- Deduplication: Tự động lọc trùng lặp nếu câu hỏi (unaccented) xuất hiện trong vòng 24 giờ.
- TTL Index: Lịch sử tự động xóa sau 90 ngày (cấu hình qua `FAQ_LOG_RETENTION_DAYS`) nhờ MongoDB TTL.

11.3. AUTO FAQ SYNTHESIS (Tự động tổng hợp FAQ):
- Gom cụm (Clustering): Gom các lịch sử tương tác có cùng Metadata và có độ tương đồng ngữ nghĩa cao (threshold `0.85`).
- Generation: Dùng LLM (Gemini) đóng vai "Senior Academic Advisor" để phân tích cụm, tự động đặt câu hỏi tổng quát và soạn câu trả lời chuẩn.
- Admin Review Workflow: Các FAQ được tổng hợp sẽ nằm ở trạng thái `pending`. Admin cần Review (Approve/Reject) và có thể Override nội dung trước khi xuất bản thành FAQ chính thức.

11.4. FORMS MANAGEMENT MODULE (Quản lý biểu mẫu):
- Module này quản lý danh sách các liên kết biểu mẫu và quy trình học thuật, giúp sinh viên/giảng viên truy cập nhanh các mẫu đơn cần thiết.
- **Unified Schema**: Gộp tên hiển thị và đường dẫn thành một trường Rich Text (HTML), cho phép AI và người dùng linh hoạt trong việc trình bày.
- **Link Processing**: Mọi liên kết trong Rich Text được tự động xử lý bởi Frontend (hàm `fixRichTextLinks`) để mở trong tab mới, đảm bảo tính nhất quán với module FAQ.
- **Bulk Import**: Nạp hàng loạt biểu mẫu từ Excel/CSV với cơ chế chuẩn hóa số liệu (ví dụ: `2020.0` -> `2020`) và bảo toàn định dạng Rich Text.
- **Shared Excel Integration**: Sử dụng module `app/integrations/excel/utils.py` để dùng chung logic tìm kiếm cột cho cả Excel và CSV.

11.4. HYBRID SEARCH STRATEGY (Tìm kiếm kết hợp):
- Hệ thống áp dụng chiến lược Hybrid Search cho FAQ để đảm bảo độ chính xác tối đa:
  - **Full-Text Search (FTS)**: Sử dụng MongoDB `$text` index. Hệ thống sử dụng tìm kiếm mềm dẻo (không bao ngoặc kép), ưu tiên kết quả khớp từ khóa chính xác nhưng vẫn hỗ trợ tìm kiếm linh hoạt (vd: "GPA", "học bổng").
  - **Semantic Match**: Sử dụng Qdrant Vector Search để tìm các câu hỏi có cùng ý nghĩa nhưng dùng từ ngữ khác nhau.
- Sắp xếp (Ranking): Khi người dùng sử dụng tham số `search`, hệ thống ưu tiên hiển thị kết quả theo `textScore` (trọng số liên quan) từ MongoDB FTS.

11.5. PIPELINE TÍCH HỢP:
- Chat Service / Inquiry Service: Trước khi gọi RAG Agent, hệ thống kiểm tra `find_best_match` từ kho FAQ. Nếu tìm thấy (`threshold >= 0.90`), trả lời ngay lập tức (Source: `faq`) bỏ qua LLM calls. Nếu không, tiến hành RAG và lưu log (Async fire-and-forget).

Quy trình \"Nạp kiến thức\" cực kỳ chi tiết của hệ thống:

BƯỚC 1: UPLOAD & QUICK ACK (`FileService.upload_file_quick`)
- Nhận file qua FastAPI multipart.
- Validate: Max 20MB, đuôi file (.pdf, .md, .docx, .html). Mime type check.
- Metadata check: Bắt buộc có enrollmentYear, academicYear, và type. Các giá trị mặc định được sử dụng nếu thiếu.
- R2 Storage: Đẩy file binary lên storage.
- MongoDB: Tạo record ban đầu status=\"uploading\". Trả về Object thông tin file ngay lập tức.

BƯỚC 2: BACKGROUND PROCESSING (`FileService.process_file_background`)
- Download file từ R2 vào thư mục temp máy chủ.
- LlamaParse: Bóc tách file (vd: PDF, TXT, DOCX) sang Markdown chất lượng cao, giữ được bảng biểu/thời khóa biểu.
- PageIndex (`_ingest_to_vector_db`):
  - Phân tích MD để tạo cấu trúc mục lục.
  - Sinh ra 1 câu `doc_description` (Mô tả tài liệu).
  - Lưu TOC Tree vào `file_toc_trees` Collection.
- Qdrant Indexer:
  - Chia nhỏ văn bản (Semantic Chunking).
  - Gemini Embedding -> Chuyển văn bản thành dãy số (vector).
  - Push lên Qdrant kèm Payload: {fileId, text, metadata_filters}.
- Artifact storage: Upload bản `.md` đã bóc tách lên R2 (sát cạnh file gốc) để AI tham chiếu nhanh.
- Status Update: Chuyển file sang status=\"ready\" (hoặc \"failed\" nếu lỗi).

BƯỚC 3: METADATA SYNC (Atomic Counter & Qdrant Update)
- Khi file chuyển sang \"ready\", hệ thống gọi `$inc: { \"total_files\": 1 }` cho tất cả nhãn metadata tương ứng trong `metadata_types`.
- Khi user cập nhật metadata qua API `PATCH /api/files/{id}`, hệ thống đồng thời cập nhật MongoDB và sync lại payload sang Qdrant để đảm bảo bộ lọc (filters) luôn chính xác.


================================================================================
13. HE THONG METADATA TYPES (DEFINITIONS & RULES)
================================================================================

Hệ thống Metadata giúp AI \"lọc\" tài liệu một cách chuẩn xác:

PHÂN LOẠI METADATA (Hardened Schema):
- enrollmentYear: Kiểu YearRange (fromYear, toYear). Mặc định 0-9999. Hỗ trợ nhập một phía (vd: chỉ nhập fromYear thì toYear=9999).
- academicYear: Kiểu YearRange (fromYear, toYear). Tương tự enrollmentYear.
- type: Kiểu mảng (List[DocumentType]). Hỗ trợ lọc nhiều loại tài liệu cùng lúc bằng toán tử $in (Mongo) hoặc MatchAny (Qdrant).

QUY TẮC VALIDATION (MetadataService):
- Hệ thống sử dụng một Schema cứng (Hardened Schema) định nghĩa sẵn các trường metadata.
- Mọi API Request/Response đều bắt buộc sử dụng camelCase (vd: enrollmentYear thay vì enrollment_year).
- Metadata object-based: Các trường năm học được bọc trong object YearRange để hỗ trợ tìm kiếm khoảng giao thoa.
- Defaults: Nếu không cung cấp metadata khi upload, hệ thống tự động gán giá trị mặc định an toàn (0, 9999, cong_van).
11.2. ATOMIC COUNTER LOGIC (IDEMPOTENCY):
Hệ thống sử dụng cơ chế cập nhật bộ đếm nguyên tử để tránh sai lệch dữ liệu:
- Tăng bộ đếm: Chỉ thực hiện khi lệnh `find_one_and_update` chuyển trạng thái file thành công từ `PROCESSING` -> `READY`.
- Giảm bộ đếm: Chỉ thực hiện khi lệnh `find_one_and_delete` xác nhận file bị xóa đang ở trạng thái `READY`.
- Công nghệ: Sử dụng `$inc` của MongoDB để đảm bảo tính nhất quán ngay cả khi có nhiều request đồng thời.


================================================================================
14. FILTER BUILDER LOGIC (VECTOR QUERY MAPPING)
================================================================================

Chịu trách nhiệm chuyển đổi JSON Metadata Filter sang cú pháp Vector Query của Qdrant.

CƠ CHẾ LỌC DỮ LIỆU:
Hệ thống tin tưởng vào xác thực người dùng ở tầng API Gateway. Khi đã vào đến AI Service, mọi người dùng hợp lệ đều có quyền truy vấn kho tài liệu chung.
FilterBuilder xây dựng query dựa trên `metadataFilter` do người dùng cung cấp (nếu có).

THUẬT TOÁN RANGE-OVERLAP:
- Hệ thống hỗ trợ tìm kiếm theo khoảng năm: `enrollmentYear: {"fromYear": 2020, "toYear": 2022}`.
- Hỗ trợ dải năm học không đầy đủ: 
    - Nếu chỉ có `fromYear`: Tìm kiếm [fromYear, 9999].
    - Nếu chỉ có `toYear`: Tìm kiếm [0, toYear].
- Logic: Tìm kiếm các tài liệu có khoảng năm giao thoa với khoảng yêu cầu.


================================================================================
15. RAG SERVICE (STRATEGY & SEARCH FLOW)
================================================================================

Tách biệt hoàn toàn việc \"Tìm mảnh\" và \"Tạo context\".

QUY TRÌNH TRUY VẤN (PAGEINDEX SEMANTIC SEARCH):
1.  Embedding Query: Chuyển câu hỏi sang Vector.
2.  Vector Search (Coarse Search): Yêu cầu Qdrant trả về Top 30 mảnh (Chunks) liên quan nhất.
3.  PageIndex Semantic Reranking:
    - Gom nhóm các Chunks theo `fileId`.
    - Tính điểm Document Score cho mỗi tệp theo công thức: 
      $DocScore = \sum ChunkScore / \sqrt{N + 1}$.
    - Sắp xếp lại danh sách tệp dựa trên DocScore.
4.  Context Aggregation:
    - Chọn Top 5 File có DocScore cao nhất.
    - Fetch `doc_description` từ MongoDB.
5.  Injection: Đẩy danh sách File IDs và Descriptions vào Prompt để Agent thực thi vòng lặp công cụ.


================================================================================
16. PAGEINDEX INTEGRATION — AGENTIC TOOLS
================================================================================

PageIndex cung cấp 3 công cụ \"mắt thần\" cho AI Agent, giúp nó chủ động điều hướng tài liệu lớn:

1.  TOOL: `get_document(doc_id: str)`
    - Mô tả: Lấy thông tin chứng minh thư của file.
    - Output: {name, page_count, description}.

2.  TOOL: `get_document_structure(doc_id: str)`
    - Mô tả: Yêu cầu PageIndex trả về mục lục MD phân cấp.
    - Ý nghĩa: AI dùng cái này để biết thông tin tuyển sinh nằm ở Chương 2, dòng 100-200.

3.  TOOL: `get_page_content(doc_id, pages)`
    - Mô tả: Đọc text thô tại các trang/dòng cụ thể.
    - Ý nghĩa: Giúp AI lấy được con số chính xác (vd: 50% học phí) từ văn bản mà không cần nạp context khổng lồ.


17. CHAT SERVICE & AGENTIC RAG LOGIC
================================================================================

Sử dụng vòng lặp `agent_loop` tích hợp lớp bảo vệ tối ưu:

B0. AUTO-EXTRACT METADATA: 
    - Nếu người dùng không gửi filter, hệ thống tự bóc tách "Khóa" hoặc "Năm học" từ câu hỏi (ví dụ: "K22" -> enrollmentYear: 2022).

B1. FAQ PRE-CHECK:
    - Tìm kiếm nhanh trong FAQ Vector DB. Nếu trúng (Hit) với độ tin cậy cao -> Trả về câu trả lời FAQ ngay lập tức.

B2. RAG GATE (GATEKEEPER):
    - Hệ thống sử dụng một lớp LLM phân tích nhanh ý định người dùng.
    - Nếu câu hỏi là xã giao (Chào hỏi, cảm ơn) hoặc không cần dữ liệu chuyên môn -> Trả về trực tiếp.
    - Lợi ích: Tiết kiệm Token, giảm độ trễ.

B3. Input: Câu hỏi + Chat History + Metadata Context.
B4. Loop:
    - AI Agent phân tích câu hỏi. Nếu cần đọc tài liệu, nó sẽ gọi Tool.
    - PageIndex thực thi tool -> Trả lại kết quả văn bản.
    - AI Agent phân tích văn bản -> Nếu chưa đủ, lại gọi Tool tiếp; nếu đủ, sinh câu trả lời.
B5. Final Answer Construction:
    - Câu trả lời được định dạng Markdown.
    - Chèn trích dẫn `(^Tên mục lục)` ngay sau khi dùng xong thông tin từ một mục tài liệu (không chèn ở từng câu).
    - Danh sách `sources` trích dẫn gồm: fileId, title, page_numbers.
    - Hệ thống dùng `r2_storage.generate_presigned_url` tạo link tải file "nóng" (expire sau 3600s) cho sv.
    - Với Inquiry Email: Tự động chuyển đổi `(^...)` thành `(Xem thêm tại [Tên mục lục](url_file_gốc))`.

15.1. SSE (SERVER-SENT EVENTS) PROTOCOL:
- Content-Type: `text/event-stream`.
- Format: `data: {"type": "text/thought/call", "content": "...", "done": false}`.
- Final: `data: {"done": true, "sources": [...], "processingTimeMs": 1234}`.
- [NEW] Error Handling: Khi gặp lỗi 429 (Rate Limit) từ Gemini, hệ thống đẩy chunk lỗi định dạng SSE để Client hiển thị cảnh báo thay vì lỗi 500.


================================================================================
18. AUTH SERVICE & SECURITY LAYER (GRPC)
================================================================================

Dự án tuyệt đối không lưu Password. Mọi bảo mật dựa trên Nest-api gRPC.

CƠ CHẾ GUARD (RequireAuth & RequireAdmin):
1.  AI Service nhận JWT token từ Header.
2.  Gửi gRPC request: `AuthService.VerifyToken(VerifyTokenRequest(token=token))`.
3.  Nest-api thực hiện verify nội bộ. Kết quả trả về `VerifyTokenResponse`.
4.  Payload mapping:
    - `user_id` -> sub
    - `user_role` -> role (admin/lecture/student)
    - `access_metadata` -> cohort/department của chính user đó.
5.  Logic:
    - Token lỗi/hết hạn -> 401 Unauthorized.
    - Role != admin (khi gọi endpoint admin) -> 403 Forbidden.
    - Timeout/Service sập -> 503 Service Unavailable.


================================================================================
19. CAU TRUC MONGODB SCHEMA (DETAILED FIELD-BY-FIELD)
================================================================================

17.1. Collection: `files`
  - _id                 : ObjectId (Phát sinh bởi Mongo)
  - display_name        : str (Tên đẹp hiển thị UI)
  - original_filename   : str (Tên file vật lý lúc lên R2)
  - storage_path        : str (Đường link tới file gốc R2)
  - markdown_storage_path: str (Đường link tới bản bóc tách .md R2)
  - storage_bucket      : str (Tên bucket R2)
  - file_size           : int (Bytes của file gốc)
  - markdown_file_size  : int (Bytes của file MD)
  - mime_type           : str (vd: application/pdf)
  - table_of_contents   : list (Mảng các tiêu đề văn bản phẳng)
  - summary             : str (Tóm tắt ngắn gọn nội dung tài liệu)
  - customMetadata     : dict (Dạng {"academicYear": {"fromYear": 2024}, "enrollmentYear": {"fromYear": 2020, "toYear": 2020}, "type": "cong_van"})
  - status              : str (uploading, processing, ready, failed)
  - createdAt / updatedAt: datetime.

17.2. Collection: `file_toc_trees`
  - _id                 : ObjectId
  - fileId             : str (Mapping sang id của `files`, unique index)
  - doc_name            : str (Tiêu đề tài liệu theo PageIndex)
  - doc_description     : str (Tóm tắt 1 câu nội dung)
  - structure           : array (Mảng các node JSON cây phân cấp mục lục)

17.3. Collection: `metadata_types` (Core Schema)
  - key                 : str (Mã duy nhất, vd: academicYear, unique index)
  - displayName         : str (Tên UI, vd: Năm học)
  - description         : str (Mô tả nhãn)
  - allowedValues       : array [
      {
        "value": str (Mã giá trị),
        "displayName": str (Tên hiển thị),
        "totalFiles": int (Biến đếm tệp),
        "color": str (Mã hex),
        "isActive": bool
      }
    ]
  - totalFiles          : int (Tổng tệp mang tag cha này)
  - isSystem            : bool (Nếu true, cấm xóa nhãn này)
  - isActive            : bool (Soft delete toàn bộ nhãn)

17.4. Collection: `faqs`
  - _id                 : ObjectId
  - question            : str (Câu hỏi gốc có dấu)
  - question_unaccented : str (Câu hỏi không dấu - Dùng cho FTS)
  - answer_rich_text    : str (HTML/Rich text trả lời)
  - answer_markdown     : str (Markdown trả lời)
  - answer_unaccented   : str (Nội dung trả lời không dấu - Dùng cho FTS)
  - metadata_filter     : dict (Dạng {"academicYear": {"fromYear": 2024}, "enrollmentYear": {"fromYear": 2020, "toYear": 2020}})
  - isActive           : bool
  - viewCount          : int
  - source              : str (manual, synthesized, seed)
  - qdrant_point_id     : str (UUID map sang Qdrant vector)

17.5. Collection: `faq_candidates`
  - _id                 : ObjectId
  - question            : str
  - question_unaccented : str
  - answer_draft_rich_text : str
  - answer_draft_unaccented: str
  - status              : str (pending, approved, rejected)
  - similarity_count    : int (Số lượng log tương đồng gom cụm)
  - source_log_ids      : array (Danh sách IDs interaction logs)


================================================================================
20. REPOSITORY LAYER & BASE REPOSITORY CRUD
================================================================================

Dự án áp dụng Repository Pattern (app/repositories/base.py) để bao bọc Motor (async pymongo).

CÁC HÀM CƠ BẢN:
- `create(data)` : Thêm record, tự động gán Timestamp.
- `find_by_id(id)` : Trả về dict, map _id sang string.
- `update_by_id(id, data)` : Cập nhật bằng toán tử `$set`.
- `delete_by_id(id)` : Xóa vật lý (Hard delete).
- `find_many(filter, skip, limit, sort)` : Tìm kiếm có phân trang và sắp xếp.
- `count(filter)` : Đếm số lượng record theo tiêu chí.

LỚP KẾ THỪA ĐẶC BIỆT:
- MetadataRepository: Triển khai hàm `sync_metadata_counters` sử dụng $inc với filter động để cập nhật mảng lồng nhau (Nested array update) cho `allowed_values`.


================================================================================
21. STORAGE MANAGEMENT (CLOUDFLARE R2 & LOCAL CACHE)
================================================================================

19.1. R2 STORAGE:
- Lưu trữ 2 bucket chính: original-files và markdown-artifacts.
- Mọi truy cập file MD đều qua một lớp Cache logic.

19.2. ARTIFACT CACHING STRATEGY (PageIndex Integration):
Hệ thống sử dụng chiến lược \"Double-Fetch\" để tối ưu hiệu năng và chi phí:
1.  Check Local: Khi cần file MD (cho Agent đọc), hệ thống kiểm tra thư mục `PAGEINDEX_WORKSPACE/artifacts`.
2.  TTL Logic: File cached tại local sẽ tồn tại trong 60 phút (3600s).
3.  Background Invalidation: Một background task kiểm tra và xóa file sau khi quá hạn.
4.  R2 Fallback: Nếu không thấy local cache, hệ thống tải từ R2 về local rồi mới cung cấp cho PageIndex Tool.

Client: r2_storage (app/integrations/storage/client.py).
Giao tiếp thông qua `aioboto3` (Async AWS SDK).

LOGIC QUAN TRỌNG:
- MIME AUTO-REGISTRATION: 
  Trong `FileService`, hệ thống tự động gán Mime Types cho Word/Excel (.docx, .xlsx, .pptx) vì các hệ điều hành Linux tối giản (Docker) thường không nhận diện được các file Office mặc định.
- PATH GENERATION:
  File được lưu theo: `uploads/YYYY/MM/timestamp_original.pdf`. Ngăn chặn việc ghi đè nếu hai người cùng upload file tên \"tailieu.pdf\".


================================================================================
22. RABBITMQ CONSUMER (CONSUMER RESILIENCE)
================================================================================

- Thread Model: Chạy trên `threading.Thread` riêng.
- Retry Loop: 
  Trong hàm `_run()`, nếu connection RabbitMQ bị mất, hệ thống dùng vòng lặp For 1-10 lần, delay tăng dần (`min(5s * attempt, 60s)`) để thử kết nối lại.
- Poison Loop Protection: Nếu parse JSON payload hỏng ngay từ đầu -> Ack message luôn (loại bỏ khỏi hàng chờ) chứ không requeue để tránh kẹt queue mãi mãi.


================================================================================
23. HE THONG EXCEPTION HIERARCHY
================================================================================

Mỗi Exception được map vào một mã lỗi HTTP tương ứng trong Middleware hoặc trả về trực tiếp.

MÃ LỖI & Ý NGHĨA:
- 400 Bad Request (ValidationException): Upload sai định dạng, metadata thiếu.
- 401 Unauthorized: JWT token sai, gRPC Nest-api reject token.
- 403 Forbidden (ForbiddenException): Student cố tình gọi API Admin, Xóa system metadata.
- 404 Not Found (NotFoundException): Không tìm thấy File, Metadata Key.
- 409 Conflict (ConflictException): Upload file trùng tên, Xóa nhãn đang có tài liệu sử dụng.
- 500/502/503 (StorageException, GeminiException, RabbitMQException): Lỗi hạ tầng hoặc API bên thứ ba.


================================================================================
24. DANH LUC REST API ENDPOINTS ĐẦY ĐỦ
================================================================================

PHÂN HỆ FILES:
- GET /api/files                : List file (Metadata search, Keyword search, Phân trang).
- POST /api/files               : Upload file (Quick flow, trả về fileId ngay).
- GET /api/files/{id}           : Chi tiết file (Metadata, status).
- PATCH /api/files/{id}         : Cập nhật tên hiển thị và metadata (Sync Qdrant).
- GET /api/files/{id}/download   : Download file (Stream dữ liệu trực tiếp từ Server).
- DELETE /api/files/{id}        : Xóa file (Reverse cleanup).

PHÂN HỆ CHAT:
- POST /api/chat/query          : Chat QA đồng bộ.
- POST /api/chat/stream         : Chat QA streaming (SSE).

PHÂN HỆ METADATA (Hardened):
- GET /api/metadata/schema      : Lấy định nghĩa Schema metadata hiện tại của hệ thống.

PHÂN HỆ HEALTH & SYSTEM:
- GET /health                   : Trạng thái kết nối DB, R2, RabbitMQ, gRPC.
- GET /                         : Root check.
- POST /api/email/process       : Manual trigger Email Orchestrator (dùng cho debug/test).
- POST /api/email/test/ingested  : Test RabbitMQ payload via HTTP.



================================================================================
25. REAL-TIME NOTIFICATIONS (WEBSOCKET)
================================================================================

Hệ thống cung cấp kênh WebSocket `/api/files/progress/{clientId}` để theo dõi các bước xử lý file nặng.

CO CHE HOAT DONG:
1. Singleton `FileStatusNotifier` lưu trữ các kết nối đang hoạt động.
2. Khi `FileService` thực hiện các bước (R2 upload, LlamaParse, PageIndex), nó gọi `progress_callback`.
3. Callback này gửi JSON payload tới các client đang lắng nghe clientId tương ứng.
4. Payload Format: `{"step": "string", "message": "string", "fileId": "string"}`

================================================================================
26. CAU HINH HE THONG (SETTINGS TABLE & .env IMPACTS)
================================================================================

Các biến môi trường quan trọng:
- `AGENT_MAX_TURNS`: Số vòng lặp tối đa của RAG Agent (Mặc định: 7). Tránh Agent bị lặp vô tận.
- `MONGODB_URL`: Link kết nối cơ sở dữ liệu.
- `R2_BUCKET_NAME`: Tên kho lưu trữ tài liệu.

| Biến .env             | Giá trị mặc định     | Mô tả |
| :---                  | :---                 | :---  |
| GOOGLE_API_KEY        | (Required)           | Dùng cho Gemini Embedding & Chat |
| MONGODB_URL           | mongodb://...        | Connection string |
| QDRANT_URL            | http://localhost:6333| Địa chỉ Vector DB |
| PAGEINDEX_WORKSPACE   | storage/workspace    | Nơi cache Markdown & Tree JSON |
| GRPC_URL              | localhost:5000       | Link tới Nest-api Backend |
| MAX_FILE_SIZE_MB      | 20                   | Chặn dung lượng upload |
| LLM_MODEL             | gemini-2.5-flash     | Model dùng cho Email Classification |
| GEMINI_MODEL          | gemini-2.5-flash     | Model dùng cho RAG Agentic Chat |
| GEMINI_EMBEDDING_MODEL| models/gemini-embedding-001 | Model dùng cho Vector Embeddings |
| LLAMA_CLOUD_API_KEY   | (Required)           | Dùng cho LlamaParse PDF Parsing |


================================================================================
27. SCRIPTS HO TRO BAO TRI (MAINTENANCE)
================================================================================

1. scripts/init_db.py: 
   Sử dụng cờ `--skip-confirm` để chạy tự động. Lệnh sẽ:
   - Thử drop database hiện tại.
   - Xóa sạch toàn bộ objects trong R2 bucket.
   - Xóa toàn bộ Collection trên Qdrant (Bao gồm files và faqs).
   - Xóa sạch thư mục workspace địa phương.
   - Khởi tạo lại Indexes MongoDB.
   - [PHASE 2.5]: Tự động gọi `scripts/seed_faqs.py` để nạp dữ liệu FAQ & Candidates mẫu.

2. scripts/seed_metadata.py:
   - Nạp Academic Year (2020 -> 2026).
   - Nạp Cohort (K62 -> K68).

3. scripts/check_qdrant.py:
   - Kết nối Qdrant và in ra số lượng points.
   - In ra ví dụ payload của 1-2 points để kiểm tra Metadata Filter có đang được đánh index đúng hay không.


================================================================================
28. AI PROMPTS TAP TRUNG HOA
================================================================================

Tất cả các prompt tại dự án được viết theo cấu trúc:
- ĐỐI TƯỢNG (Bạn là ai?)
- NGỮ CẢNH (Bạn đang giúp ai?)
- RÀNG BUỘC (Định dạng output, điều không được làm).
- HƯỚNG DẪN TRÍCH DẪN (Phải ghi rõ File ID, Trang nào).

LƯU Ý QUAN TRỌNG:
Quá trình Refactor đã chuyển các prompt từ file `app/core/prompts.py` cũ và phân mảnh vào các module riêng biệt như `agent.py`, `label_classifier_service.py`, v.v. Điều này giúp đóng gói logic gần nhau và tránh Import lỗi.
Toàn bộ Prompts phục vụ RAG đều được ép cấu trúc tư duy (reasoning) và trả lời bằng Tiếng Việt. Phân loại (Triage) và Trích xuất (Extraction) dùng Tiếng Anh để tăng độ map các Token.

--------------------------------------------------------------------------------
Xác nhận bản thảo Overview Version 4.5.0 (Extreme Detail).
--------------------------------------------------------------------------------

================================================================================
29. HE THONG SNAPSHOT & RESTORE (DATA PERSISTENCE)
================================================================================

Hệ thống hỗ trợ cơ chế Snapshot để tránh nạp lại dữ liệu qua AI tốn phí khi cần khởi tạo lại database. Snapshot bao gồm Metadata trong MongoDB và Raw Vectors trong Qdrant.

QUY TRÌNH BẢO TRÌ NHANH:

Tác vụ                | Câu lệnh
----------------------|----------------------------------------------------------------------
Sao lưu dữ liệu      | python scripts/snapshot.py export scripts/uploads_result/rag_backup.json
Khôi phục dữ liệu    | python scripts/init_db.py --restore scripts/uploads_result/rag_backup.json
Khởi tạo gốc (AI)    | python scripts/init_db.py --skip-confirm

LƯU Ý KỸ THUẬT:
- Snapshot lưu trữ trực tiếp Raw Vectors nên sẽ tuân thủ tuyệt đối cấu hình QDRANT_VECTOR_SIZE trong .env.
- Thư mục lưu trữ mặc định được khuyến nghị: scripts/uploads_result/.
- Quá trình Restore được tích hợp trực tiếp vào init_db.py qua cờ --restore, giúp dọn sạch môi trường cũ trước khi nạp Snapshot.

--------------------------------------------------------------------------------
Xác nhận bản thảo Overview Version 5.5.0 (Chat Customization & CSV support).
--------------------------------------------------------------------------------

================================================================================
30. PHU LUC: QUY CHUAN XU LY EXCEL & RICH TEXT
================================================================================

Để đảm bảo tính nhất quán giữa các module FAQ và Forms, hệ thống sử dụng một bộ tiện ích chung tại `app/integrations/excel/utils.py`.

CÁC QUY TẮC XỬ LÝ CHÍNH:
1.  Numeric Normalization: 
    - Excel thường lưu số nguyên dưới dạng float (vd: 2020 thành 2020.0).
    - Hệ thống tự động chuyển đổi các giá trị này về chuỗi số nguyên sạch (vd: "2020").
2.  Rich Text Preservation:
    - Sử dụng `openpyxl` để duyệt từng `InlineFont` fragment trong ô Excel.
    - Chuyển đổi các định dạng (Bold, Italic, Underline, Color) sang HTML tương ứng.
3.  Hyperlink Extraction:
    - Tự động trích xuất URL từ các ô có gắn Link.
    - Gộp URL và text hiển thị thành thẻ `<a>`.
4.  Security & UX:
    - Tất cả các thẻ `<a>` được gán `target="_blank"` và `rel="noopener noreferrer"` (xử lý ở tầng Frontend hoặc API Response).
5.  Pydantic Compatibility:
    - Mọi dữ liệu bóc tách từ Excel được ép kiểu về `str` trước khi đưa vào Pydantic model để tránh lỗi `Input should be a valid string`.
