syntax = "proto3";

package app_server;

service MessageService {
  rpc GetState(GetMessageStateRequest) returns (GetMessageStateResponse) {}
}

message GetMessageStateRequest {
  int32 messageId = 1;
}

message GetMessageStateResponse {
  bool isCurrent = 1;
  bool hasRecords = 2;
}

